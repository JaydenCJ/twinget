// Package version pins the single source of truth for the tool version.
package version

// Version is the semantic version of twinget. It must match go.mod's
// manifest header and CHANGELOG.md.
const Version = "0.1.0"
